replicaCount: 2

image:
  repository: dmytrobudnik/api-service
  tag: "3d55c388"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  host: api.local
  annotations: {}
  tls: []
